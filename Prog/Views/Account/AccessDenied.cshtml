@{
    ViewData["Title"] = "Access Denied";
}

<h1>Access Denied</h1>
<p>You do not have permission to access this page.</p>

@if (!string.IsNullOrEmpty(ViewBag.ReturnUrl))
{
    <p>Return URL: @ViewBag.ReturnUrl</p>
    <a href="@ViewBag.ReturnUrl">Back to Previous Page</a>
}
